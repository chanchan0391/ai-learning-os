# AI Learning OS 待办列表

## 已完成

- [x] 建立 Git 仓库和前端工程
- [x] 实现学习目标创建流程
- [x] 实现确定性 Planner Agent 领域模型
- [x] 生成阶段路线和第一天任务
- [x] 保存并恢复本地学习进度
- [x] 添加核心逻辑自动化测试
- [x] 记录 MVP 范围与产品决策
- [x] 定义 provider-neutral AI 与 Agent 架构
- [x] 建立服务端 Planner Agent API
- [x] 实现 OpenAI Responses 模型适配器
- [x] 添加结构化输出和领域校验
- [x] 保持确定性模型作为开发与评估基线

## 下一步

### P0：形成多天学习循环

- [x] 设计学习记录、任务反馈和每日状态的数据模型
- [x] 支持完成当天学习后生成下一天任务
- [x] 增加连续学习天数和历史进度视图
- [x] 为本地数据增加版本迁移和异常恢复

### P1：引入教学与评估

- [x] 重启 Codex 后验证官方 OpenAI 文档连接器
- [x] 使用用户本地凭据完成一次实时模型烟雾测试（`gpt-5.6-luna`）
- [x] 定义并实现 Teacher Agent 的教学会话契约与 API
- [x] 定义并实现 Evaluator Agent 的固定量表、结构化输出与 API
- [x] 用可替换适配器接入第一个 LLM 服务
- [x] 添加任务难度反馈并让 Planner Agent 调整后续任务
- [x] 在学习任务界面接入短教学会话、主动理解检查和成果提交
- [x] 把 Evaluator 结果写入版本化学习记录，并据此调整下一天任务

### P2：工程化

- [x] 增加组件级交互测试和可访问性检查
- [x] 为 Agent 调用增加超时、取消和幂等重试
- [x] 增加学习记录导出与删除控制
- [x] 增加学习记录导入、完整校验与替换确认
- [x] 增加持续集成工作流
- [x] 设计首个后端 API
- [x] 设计持久化模型并建立可替换的浏览器存储边界
- [x] 建立用户隔离的同步领域契约，覆盖 revision、游标和幂等重试
- [x] 选择 provider-neutral OIDC + 服务端会话身份方案
- [x] 实现 PostgreSQL 同步适配器、迁移与数据库契约测试
- [x] 建立可信身份解析边界与同步 HTTP API，覆盖条件写入、幂等、冲突和来源校验
- [x] 实现 OIDC 授权发起与登录回调（discovery、state、nonce、PKCE 和 ID Token 验证）
- [x] 实现 OIDC 身份映射后的用户/设备登记、服务端会话轮换与登出
- [x] 把 PostgreSQL 同步仓库和会话解析适配器接入服务启动配置
- [x] 接入账号与跨设备同步界面，支持会话状态、显式同步、云端恢复和冲突保护

### P3：同步体验与上线准备

- [x] 增加本地与云端冲突版本预览及保留选择
- [x] 增加自动同步队列、离线重试和最近同步时间
- [x] 为认证与同步端点增加速率限制和安全审计事件
- [x] 完成账号数据删除、隐私告知和本地恢复演练基线
- [x] 在真实 OIDC 与部署数据库环境执行恢复演练并记录结果（见 `docs/dev-recovery-drill.md`）
- [ ] 在真实浏览器完成 OIDC 授权回调、会话建立和一次跨设备同步验收
- [x] 明确并公布 MVP 备份、基础设施日志和模型提供商数据保留期限
- [x] 增加同步容量监控和多实例共享限流
- [x] 增加“退出所有设备”账号安全控制与会话撤销测试
- [x] 增加登录设备清单和单设备会话撤销控制

### P4：扩展自适应学习闭环

- [x] 建立 Review Agent 最小间隔复习：重新检索 Evaluator 识别的薄弱点
- [x] 根据复习表现调整后续间隔，而不是只使用固定间隔
- [x] 展示未来 14 天的薄弱点复习计划，并随回忆表现即时重排
- [x] 增加 Coach Agent 的学习中断识别与低压力恢复计划
- [x] 生成可检索、可编辑的阶段学习笔记
- [x] 支持手动新建阶段笔记，并在不覆盖人工编辑的前提下追加新学习证据
- [x] 为阶段笔记增加删除与导出 Markdown 控制

### P5：学习洞察与长期复盘

- [x] 增加最近 7 个完成日的证据化学习周回顾
- [x] 增加可按日期回看的完整学习日历
- [x] 为 Review Agent 增加基于主动回忆答案的自动判分
- [x] 支持导出周回顾与阶段进展摘要

### P6：纵向学习趋势

- [x] 用等长完成日窗口比较成果评分、偏难日和轻松回忆变化
- [x] 增加阶段结束回顾，把阶段目标、代表成果与待迁移能力汇总为可编辑记录
- [x] 支持从历史证据发现跨阶段重复误解，并生成关联复习提示

### P7：洞察行动化与多目标管理

- [x] 允许把跨阶段关联提示加入可完成、可判分的主动回忆任务
- [x] 支持在本地归档已完成学习目标、保留完整证据并创建新目标
- [x] 把归档状态接入账号同步，支持跨设备上传、下载与恢复已完成目标
- [x] 支持多个并行活跃目标的本地保存、迁移、新建和切换
- [x] 把多个并行活跃目标接入账号同步，移除单一云端活跃计划假设
- [x] 提供跨目标首页，汇总各目标的今日任务、风险和最近进展

### P8：跨目标节奏管理

- [x] 汇总所有进行中目标的今日剩余时间和需关注目标数
- [x] 允许学习者设置跨目标每日总时间预算，并提示超载
- [x] 提供跨目标周回顾，比较投入分配、成果进展与风险变化

### P9：跨目标回顾行动化

- [x] 允许从跨目标周回顾直接打开本周优先目标
- [x] 支持导出跨目标周回顾，便于长期组合复盘

### P10：跨目标每日执行

- [x] 按每日总预算生成跨目标原子任务清单，并优先安排恢复与复习风险
- [x] 支持从跨目标清单直接打开对应目标和任务

### P11：阶段掌握度检查

- [x] 从阶段成果评估汇总理解、应用、证据和反思四维掌握度
- [x] 区分证据不足、需要加强和可进入下一阶段，并给出唯一最小下一步
- [x] 在阶段回顾界面和学习进展 Markdown 中复用同一派生结论

### P12：阶段掌握度行动化

- [x] 允许把未达标阶段的唯一最小下一步加入当天可提交、可评估的补强实践
- [x] 在补强评估完成后，对比展示阶段掌握度变化并保留补强来源

### P13：最终阶段补强

- [x] 允许在计划结束后为未达标的最终阶段开启一个完整预算的补强学习日
- [x] 让补强学习日通过导出、同步和归档保留，同时拒绝无补强来源的计划外学习日

### P14：目标完成证据

- [x] 区分计划日程完成与全部阶段掌握度达标，并汇总目标级证据状态
- [x] 自动选择最需要补证据的已完成阶段，并把同一最小下一步写入界面和进展导出

### P15：目标掌握度行动化

- [x] 允许从目标级优先项直接开启对应阶段的补强实践，无需在阶段列表中再次查找
- [x] 复用阶段补强来源、每日预算和去重规则，确保目标入口不产生第二套学习状态

### P16：目标证据交付

- [x] 在计划完成后导出独立目标证据报告，汇总完成结论、阶段掌握度、四维证据与阶段回顾
- [x] 让报告复用目标与阶段掌握度派生逻辑，避免界面、进展摘要和交付报告产生不同结论

### P17：目标证据可审计性

- [x] 在目标证据报告中按阶段列出成果正文、Evaluator 四维证据与反馈、误解和最小下一步
- [x] 统一阶段证据归属，确保跨阶段补强只计入来源阶段且每份证据只计算一次

### P18：归档证据可携带性

- [x] 让已完成目标列表在仍有进行中目标时保持可见，并可直接导出版本化学习记录
- [x] 允许从归档直接导出目标证据报告，并保持归档与当前目标选择不变

### P19：完整学习组合备份

- [x] 一次导出全部进行中目标、归档目标和本地跨目标时间预算
- [x] 完整校验备份后经明确确认替换当前浏览器学习组合，拒绝重复目标和无效选择

### P20：非破坏性组合恢复

- [x] 从完整备份只合并本地不存在的目标，并跳过同 ID 版本
- [x] 合并时保留当前目标、已有进度和本地跨目标时间预算

### P21：组合恢复影响预览

- [x] 在执行前列出仅合并路径会新增和跳过的目标
- [x] 明确展示全部替换会移除的本地目标数量和时间预算变化

### P22：恢复回滚保护

- [x] 在全部替换前自动下载当前学习组合的版本化恢复前备份
- [x] 在单目标导入覆盖同 ID 本地或归档目标前复用同一回滚保护

### P23：同目标版本选择恢复

- [x] 并排比较同 ID 本地与备份版本的状态、学习日、任务和最近记录
- [x] 允许逐目标明确采用备份版本，同时保留未选本地版本、当前预算和文件外目标

### P24：生产与商业化就绪

- [x] 建立 dev 自动部署、迁移前备份、健康检查和失败回滚基线
- [x] 记录生产上线门槛与分阶段发布策略（见 `docs/production-readiness.md`）
- [x] 为所有 Agent 模型入口增加多实例共享限流、隐私安全审计和容量指标
- [ ] 使用托管身份提供商完成真实注册、找回、MFA 和跨设备同步验收
- [ ] 建立独立生产 CI/CD、受保护环境、集中监控告警和异地恢复
- [x] 建立账号级模型用量账本、月度 token/金额预算和成本熔断
- [x] 增加所有账号共享的应用级月度金额熔断，并区分全局与账号额度拒绝
- [x] 为每次模型响应增加可配置输出 token 上限，缩小异常与在途调用成本风险
- [x] 建立覆盖五类 Agent 的固定质量、延迟与 token 评估集，并加入确定性发布回归门槛
- [x] 将模型预算接入服务端订阅权益和套餐级 token/金额配额，未配置套餐安全拒绝
- [ ] 配置并验证独立于应用账本的模型提供商账户金额硬上限
- [ ] 完成隐私、服务条款、模型供应商数据边界和商业计费方案

### P25：生产可观测性

- [x] 为 API 响应增加服务端请求 ID，并输出不含查询参数、正文、Cookie 或学习内容的结构化请求完成日志
- [x] 让健康端点主动验证 PostgreSQL 就绪状态，并在依赖不可用时返回隐私安全的 `503`
- [ ] 将请求日志、容量指标和安全审计接入集中采集、保留策略、SLO 看板与告警值班流程

### P26：付费模型请求安全

- [x] 为使用账号预算的 Agent 入口增加精确来源校验，在预算查询、正文读取和模型调用前拒绝跨站请求
- [x] 增加订阅权益拒绝、套餐切换和宽限期的服务端契约测试

### P27：浏览器响应安全

- [x] 为 API 响应统一增加 CSP、禁止嵌入、MIME 嗅探、敏感浏览器能力和 Referrer 泄露保护
- [x] 为 dev Web 入口增加仅允许同源脚本、样式、连接和资源的 CSP 及等价安全响应头

### P28：JSON 请求边界

- [x] 所有 Agent 与同步正文端点只接受 `application/json`，阻止跨站简单请求绕过浏览器预检
- [x] 在解析前拒绝声明超过 1 MB 的正文，并保留流式上限以覆盖分块传输

### P29：同步元数据容量边界

- [x] 将同步游标和幂等结果限定为 30 天有效期，过期游标安全回退为完整同步
- [x] 增加按创建时间索引和每日机会清理，阻止高频同步导致数据库元数据无界增长

### P30：模型用量记账幂等性

- [x] 以模型提供商请求 ID 去重重放的记账步骤，避免同一响应重复消耗账号与应用预算
- [x] 为已有重复记录提供迁移收敛，并保留不返回请求 ID 的兼容 Provider 记账能力

### P31：同步读取容量边界

- [x] 将账号全量同步快照限制为每页 250 个实体，并用账号隔离的不透明游标继续读取
- [x] 让浏览器透明读取全部分页、合并并发更新的重复实体，并拒绝循环或异常分页游标

### P32：同步客户端资源保护

- [x] 浏览器拒绝超过服务端契约的 250 实体分页，避免异常响应造成无界内存增长
- [x] 将单次完整同步限制为 25,000 个不同实体和 100 页，并以安全错误停止异常快照

### P33：API 进程资源保护

- [x] 限制请求头等待、请求正文接收、空闲连接、请求头数量和单连接请求数
- [x] 让进程终止只执行一次有界排空，正常关闭依赖，超时后关闭残留连接并以失败状态退出

### P34：同步索引资源保护

- [x] 将同步用户、设备、实体、幂等操作和游标标识符限制为 1–256 个字符
- [x] 在内存与 PostgreSQL 查询前共享验证，并覆盖 HTTP 稳定错误响应

### P35：Agent 并发资源保护

- [x] 为单实例同时执行的 Agent 请求增加有界准入，在读取正文和调用模型前拒绝过载
- [x] 在健康端点暴露不含用户或学习内容的并发上限、进行中数量和累计拒绝数

### P36：就绪探针资源保护

- [x] 合并同一实例同时发生的 PostgreSQL 就绪检查，避免公开健康探针放大数据库连接与查询压力
- [x] 不缓存成功或失败结果，让下一批健康检查重新验证依赖并及时发现恢复或故障

### P37：Agent 对抗性评分评估

- [x] 为 Evaluator 和 Review 增加内嵌提示词、指定满分和提示词泄露请求的固定对抗案例
- [x] 明确把学习者内容视为不可信证据，并让确定性发布基线拒绝以指令长度冒充能力证据

### P38：Agent 发布资源门槛

- [x] 为固定评估集增加单案例延迟、单案例输出 token 和整组总 token 的机器可判定上限
- [x] 让超限或实时用量缺失的评估以隐私安全的错误类型和违例代号失败，并支持通过环境变量收紧发布预算

### P39：Agent 输入成本边界

- [x] 将五类付费 Agent 的单次 JSON 请求限制为 64 KiB，同时保留同步数据的 1 MB 契约
- [x] 在模型调用前同时拒绝超限的声明长度与分块传输正文，避免异常学习内容放大输入 token 成本

### P40：客户端断开可观测性

- [x] 在浏览器中断 Agent 请求时记录隐私安全的 499 请求日志与安全审计原因
- [x] 在连接提前关闭时结算容量指标，避免长期运行实例的进行中请求计数泄漏

### P41：反向代理限流身份边界

- [x] 默认忽略可伪造的转发地址，只允许显式受信直连代理提供客户端限流地址
- [x] 使用最靠近应用的有效转发地址，并在配置或请求异常时安全回退到直连地址

### P42：单实例限流状态容量边界

- [x] 将无数据库运行模式的内存限流状态限制为 10,000 个客户端与路由范围组合
- [x] 达到上限时先回收过期窗口，并对新的高基数身份安全拒绝而不是继续分配内存

### P43：可观测路径隐私边界

- [x] 将同步计划、每日记录和设备撤销路径中的用户控制标识替换为稳定路由模板
- [x] 将未匹配路径统一记录为低基数标签，避免攻击者输入进入请求日志或安全审计

### P44：认证元数据容量边界

- [x] 将过期会话和无活跃会话的废弃设备限定为 30 天保留，避免登录与令牌轮换长期放大数据库状态
- [x] 增加按过期时间和最后活跃时间索引，并以每日机会清理安全收敛历史认证元数据

### P45：上游响应资源边界

- [x] 对模型提供商 JSON 响应实施 1 MB 流式上限，并在超限时以稳定网关错误停止重试
- [x] 对 OIDC discovery 与令牌响应实施独立的小型流式上限，避免身份上游异常放大 API 内存

### P46：模型调用总时限

- [x] 为模型请求增加跨重试、响应读取和退避等待的 60 秒默认总时限
- [x] 支持启动时校验的显式总时限配置，并保留客户端取消与单次尝试超时语义

### P47：数据库进程资源保护

- [x] 为 API PostgreSQL 连接池增加连接数、获取等待、空闲连接和连接生命周期边界
- [x] 为运行时数据库语句、客户端等待和空闲事务增加独立时限，并在启动时拒绝无效配置

### P48：数据库连接容量可观测性

- [x] 在健康端点公开不含账号、查询或连接信息的连接池上限、打开、空闲、使用中和等待数量
- [x] 明确标记连接池满载或已有连接等待者的饱和状态，并覆盖异常计数的安全收敛

### P49：进程关闭总时限

- [x] 让同一关闭截止时间同时覆盖 HTTP 排空与数据库等依赖清理
- [x] 依赖清理超时后以失败状态只退出一次，避免部署或回滚被无限期阻塞

### P50：同步响应字节边界

- [x] 在 250 实体分页上再增加 8 MiB 编码字节边界，并用游标安全续读剩余实体
- [x] 浏览器流式读取同步分页并在 9 MiB 响应上限处取消，避免异常服务端造成无界内存增长

### P51：浏览器 API 响应资源保护

- [x] 将五类 Agent JSON 响应限制为 1 MiB，并在声明或流式响应超限时取消读取
- [x] 将认证 JSON 响应限制为 64 KiB，并复用有界读取覆盖同步写入与分页响应

### P52：认证设备容量边界

- [x] 将单账号同时活跃设备限制为默认 100 个，并允许通过启动配置收紧
- [x] 在注册新设备前清理已无有效会话的设备，并串行化同账号登录以阻止并发绕过上限

### P53：OIDC 上游时限一致性

- [x] 让 discovery、授权码交换和 JWKS 密钥获取共享可配置的请求时限
- [x] 启动时拒绝无效或超过 60 秒的身份上游时限，避免运维配置重新引入无界等待

### P54：数据库迁移发布安全

- [x] 使用 PostgreSQL advisory lock 串行化迁移运行，避免并发发布重复应用同一迁移
- [x] 记录并验证迁移 SQL 的 SHA-256，兼容回填既有记录并拒绝已应用历史被静默改写

### P55：OIDC 密钥响应资源边界

- [x] 将远程 JWKS 响应限制为 256 KiB，并在签名验证解析前取消超限流
- [x] 让 JWKS 获取复用受控 HTTP 客户端与既有上游时限，避免绕过测试和部署网络边界

### P56：上游凭据重定向保护

- [x] 禁止 OIDC discovery、令牌交换和 JWKS 请求自动跟随重定向，避免授权码或身份请求被转发到非预期主机
- [x] 禁止模型提供商请求携带 Bearer 密钥自动跟随重定向，并覆盖原生与兼容 Provider 契约测试

### P57：上游 URL 凭据边界

- [x] 拒绝在兼容模型服务、OIDC issuer 和回调 URL 中嵌入用户名或密码，避免配置泄密与 URL 身份歧义
- [x] 拒绝 OIDC discovery 返回带嵌入凭据的授权、令牌和 JWKS 端点，并覆盖三类端点回归测试

### P58：CI 供应链完整性

- [x] 将 GitHub Actions 固定到不可变提交，并禁止 checkout 把短期凭据保留在 Git 配置中
- [x] 增加 CI 总时限和每周依赖更新检查，避免卡住的任务与长期未审查的依赖漂移

### P59：依赖运行时可复现性

- [x] 用显式兼容范围替换生产与构建依赖的浮动 `latest`，并把 Node 主版本边界写入包契约
- [x] 统一 CI、文档与 dev 部署使用 Node 22，升级通过验证的依赖并保留不兼容的 jsdom 主版本

### P60：依赖变更审查

- [x] 在拉取请求中审查新增依赖的已知漏洞、运行范围与许可证，并将审查 Action 固定到不可变提交
- [x] 在每次 CI 中审计完整锁文件，阻止已解析依赖中的高危与严重漏洞

### P61：Node 22 小版本升级

- [x] 将本地与 dev 运行时升级到 Node 22.23.1，并统一 `.nvmrc`、包契约、用户服务和部署脚本
- [x] 升级到 jsdom 30 并移除临时 Dependabot 忽略，在新的最低 Node 版本契约下重新验证完整测试

### P62：dev 运行时漂移保护

- [x] 在安装前确认 npm 实际使用选定的 Node，并在健康门中验证 API 与 Web 进程二进制路径
- [x] 在成功部署后原子更新远程部署运行器，并记录用户服务 unit 变更的备份与安装要求

### P63：dev 控制面变更安全

- [x] 提供只读状态命令，统一检查用户服务 unit 漂移、启用/运行状态与实际 Node 进程路径
- [x] 提供串行化、带备份和失败回滚的 unit 原子安装命令，并覆盖成功、漂移与回滚测试

### P64：GitHub 依赖图门禁恢复

- [ ] 由仓库管理员在 Advanced Security 设置中启用 Dependency Graph，使依赖变更审查不再以“不支持此仓库”失败
- [ ] 重新运行并通过 Dependabot PR #7 的 dependency-review job，再合并固定到不可变提交的 v5 Action 升级

### P65：模型用量缺失保护

- [x] 在 Responses 或兼容 Provider 缺少 token 用量时按请求与结构化输出 UTF-8 字节数保守记账
- [x] 让其他实时 Provider 在认证预算上下文缺少用量时安全失败，避免新增适配器静默绕过成本熔断

### P66：实时模型启动成本保护

- [x] 实时模型默认要求账号用量账本和认证预算，避免凭据误配置成公开未认证、未计量的 Agent API
- [x] 仅为隔离的本地烟雾测试保留显式不计量开关，并拒绝含糊布尔配置

### P67：模型运行配置硬边界

- [x] 将结构化模型输出配置限制在最多 32,768 token，避免错误运维值放大单次成本
- [x] 将跨重试模型调用总时限限制在最多 120 秒，避免配置重新引入长期占用

### P68：数据库传输机密性

- [x] 非回环 PostgreSQL 连接必须显式启用证书和主机名验证，避免账号与学习数据明文或被中间人读取
- [x] 拒绝连接 URL 中含糊或较弱的 TLS 参数，并保持本地 dev 回环数据库无需证书即可运行

### P69：运行时资源配置硬边界

- [x] 为 Agent 并发、数据库连接池和数据库等待时限增加启动时硬上限，避免错误配置放大资源占用或故障恢复时间
- [x] 为单账号活跃设备配置增加硬上限，并覆盖所有边界的启动拒绝测试

### P70：高基数启动配置硬边界

- [x] 限制允许来源与可信代理清单的总字符数和去重后条目数，避免错误配置放大启动解析与逐请求匹配成本
- [x] 限制套餐预算数量、JSON 大小、Cookie 名称及 OIDC URL、客户端 ID 和事务密钥长度，并覆盖启动拒绝测试

### P71：dev 用户服务沙箱

- [x] 为 API 与 Web systemd 用户服务增加只读文件系统、私有临时目录/设备、禁止提权与 capabilities、内核接口保护及地址族限制
- [x] 让控制面拒绝缺少沙箱基线的源 unit，并覆盖完整安装与失败回滚不会丢失或误留指令
