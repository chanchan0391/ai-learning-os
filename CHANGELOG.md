# AI Learning OS 更新记录

这是项目内部工作日志，主要记录每次更新做了什么、部署到了哪里，以及验证结果。它不是面向终端用户的发行说明。

## 2026-08-08

### 可观测路径隐私边界

- 请求完成日志与安全审计现在只记录稳定路由模板；同步计划、每日记录和设备撤销路径不再携带用户控制标识。
- 未匹配路径统一记录为 `/unmatched`，避免探测字符串、私密内容或高基数攻击输入进入遥测系统。

### 单实例限流状态容量边界

- 无数据库运行模式的内存固定窗口限流器现在最多保留 10,000 个客户端与路由范围组合，避免高基数来源让进程状态无界增长。
- 容量满时先回收过期窗口；若仍满则对未见身份安全拒绝并返回最早可重试时间，已有身份的当前窗口不受影响。
- 完整校验：28 个测试文件、265 项测试全部通过，生产构建成功；九项确定性 Agent 评估全部通过。

### 客户端断开可观测性

- Agent 请求的客户端提前断开时，现在以隐私安全的 `499` 和稳定终止原因结算请求日志与安全审计。
- 容量指标会同时释放进行中计数并记录拒绝结果，避免长期运行实例因断开连接产生观测计数泄漏。

### Agent 输入成本边界

- 五类 Agent 的单次 JSON 正文现在限制为 64 KiB，在模型调用前拒绝超限输入；同步数据仍保留原有 1 MB 契约。
- 声明 `Content-Length` 和分块传输都执行同一流式上限，避免通过省略长度头放大输入 token 与进程内存成本。
- 完整校验：28 个测试文件、259 项测试全部通过，生产构建成功；九项确定性 Agent 评估全部通过。

### Agent 发布资源门槛

- 固定 Agent 评估现在会在单案例超过 30 秒、单案例输出超过 4096 token、整组超过 100,000 token 或实时 Provider 缺少用量时失败，不再只报告资源消耗。
- 三项门槛可通过显式环境变量收紧；JSON 报告只新增数值门槛与稳定违例代号，不暴露 Prompt、响应或学习内容。

### Agent 并发资源保护

- 每个 API 实例默认最多同时执行 20 个 Agent 请求，满载时在认证预算查询、学习正文读取和模型调用前返回带 `Retry-After` 的 `503`。
- 请求完成、失败或客户端断开都会幂等释放名额；健康端点新增不含用户或学习内容的并发上限、进行中数量和累计拒绝数。
- 可通过 `AI_MAX_CONCURRENT_AGENT_REQUESTS` 调整实例上限，启动时拒绝零、负数、小数或非数字配置。
- 完整校验：27 个测试文件、251 项测试全部通过，生产构建成功；七项确定性 Agent 评估全部通过。

## 2026-08-04

### 安全审计标识最小化

- 默认 JSON 行安全审计不再输出原始账号和设备 ID，改为带类型域分隔的稳定 SHA-256 截断伪名引用，在保留事件关联能力的同时减少日志中的直接标识。
- 文档明确伪名引用仍属于受限运维数据，继续受 14 天保留期限和生产访问控制约束；自定义进程内审计接收器契约保持不变。

## 2026-08-03

### 模型用量记账幂等性

- 模型提供商返回请求 ID 时，用量账本现在以提供商和请求 ID 唯一标识响应；重放同一记账步骤不会重复消耗账号或应用级预算。
- 新迁移在建立唯一约束前收敛已有重复记账；不返回请求 ID 的 compatible Provider 保持逐次记账。

### 同步元数据容量边界

- PostgreSQL 同步游标和幂等结果现在只在 30 天内有效，服务实例每天至多执行一次过期清理，避免频繁同步让可重建元数据无界增长。
- 新迁移为两类清理增加创建时间索引；过期游标返回既有稳定错误并可通过完整同步恢复，不删除学习计划或每日学习记录。
- 增加时间推进契约测试，覆盖过期游标拒绝、旧幂等操作不重放、元数据清理和学习数据保留。

### 套餐级模型预算

- 服务端订阅权益现在把套餐键传入模型用量账本，并按 `AI_PLAN_BUDGETS_JSON` 中的套餐级月度 token 与金额配额熔断；套餐切换下一次请求生效，未配置套餐安全拒绝。
- 订阅强制模式要求显式套餐配额映射，并增加套餐选择、切换传递和错误配置测试。

### 隐私安全的请求可观测性

- API 响应现在包含服务端生成的 `X-Request-Id`，便于把用户报告与服务端完成事件关联。
- 数据库运行时输出结构化 JSON 行请求日志，只记录路径、状态、结果和延迟，不记录查询参数、正文、Cookie、用户标识或学习内容。
- 集中采集、保留策略、SLO 看板和告警仍属于生产上线门槛。

### 单次模型输出成本上限

- OpenAI Responses 与 compatible Chat Completions 请求现在默认限制为 4096 个输出 token，避免结构化 Agent 响应在异常情况下无界消耗输出额度。
- 可用 `OPENAI_MAX_OUTPUT_TOKENS` 显式调整；启动时拒绝非正整数或未配置模型 Provider 的孤立上限。
- 文档明确单次上限只缩小输出侧风险，不能消除并发预算检查窗口、输入成本或替代厂商硬金额上限。
- 完整校验：25 个测试文件、215 项测试全部通过，生产构建成功；七项确定性 Agent 评估全部通过。

### Agent 发布评估基线

- 新增覆盖 Planner、Teacher、Evaluator、Review 和 Coach 的七案例合成评估集，直接复用生产 Agent 编排、结构化输出和领域校验。
- 报告只保留案例状态、错误类型、延迟、模型与 token 总量，不输出 Prompt、输入、模型响应或请求 ID。
- 默认命令强制使用零成本确定性 Provider；实时模型评估必须显式授权，避免本地凭据或无人值守任务意外产生费用。
- 完整校验：25 个测试文件、212 项测试全部通过，生产构建成功；七项确定性 Agent 评估全部通过。

### 应用级模型总成本熔断

- 可选的全局月度金额上限现在通过时间索引汇总所有账号的模型用量；达到上限后，在读取学习正文和调用模型前统一拒绝新 Agent 请求。
- 响应头和安全审计会区分账号额度与应用全局额度；配置仍复用不含学习正文的 PostgreSQL 用量账本。
- 明确应用级检查不能替代厂商侧硬金额上限，且同一时间在途调用仍可能造成有限超额。

### 账号模型用量与预算

- OpenAI Responses 与兼容模式现在标准化返回输入/输出 token；成功 Agent 调用按认证账号、动作、Provider 和模型写入不含学习正文的 PostgreSQL 用量账本。
- 可通过显式月度 token、金额和输入/输出费率配置启用调用前成本熔断；启用后五类 Agent 入口要求有效登录，耗尽配额时在读取正文和调用模型前返回重置时间。
- 账号删除会级联清除用量事件；文档明确当前只对成功响应记账、在途调用可能有限超额，订阅权益、全局厂商金额上限和集中告警仍是生产待办。
- 完整校验：24 个测试文件、209 项测试全部通过，生产构建成功。

### Agent 成本与容量保护

- 计划、教学、成果评估、复习评估和恢复计划入口现在分别使用多实例共享的分钟级客户端配额，拒绝发生在读取学习正文和调用模型之前。
- 安全审计只记录 Agent 动作、路径、状态和拒绝原因；健康端点按五类 Agent 暴露滚动请求量、失败、限流和延迟，不包含学习内容。
- 明确这是一层突发滥用保护，生产仍需账号级长期用量账本、订阅配额、金额预算和集中告警。
- 增加模型零调用、配额响应头、审计隐私和容量范围的 HTTP 回归测试。

### 组合恢复影响预览

- 完整学习组合确认框会在写入前列出仅合并路径将新增与跳过的目标，并显示全部替换会移除的本地目标数量和时间预算变化。
- 预览从当前浏览器状态和已验证备份确定性派生，不保存第二份恢复状态，也不改变现有合并与替换语义。
- 增加领域预览和界面交互测试，覆盖新增、同 ID 保留、本地独有归档和预算变化。
- 完整校验：23 个测试文件、198 项测试全部通过，生产构建成功。

### 非破坏性组合恢复

- 完整学习组合导入新增“仅合并新目标”，只追加本地不存在的目标 ID，跳过本地同 ID 版本。
- 合并不改变当前目标、本地学习进度或跨目标时间预算；仍保留明确的“全部替换”路径。
- 增加仓库边界与界面交互测试，覆盖进行中目标、归档目标、重复版本和预算保留。
- 完整校验：23 个测试文件、197 项测试全部通过，生产构建成功。

### 完整学习组合备份

- 新增版本化“全部数据”JSON，一次包含所有进行中目标、归档目标、当前选择和本地跨目标时间预算。
- 导入前完整校验学习状态、归档完成度、目标 ID 唯一性、当前目标和预算；经明确确认后才替换当前浏览器的学习组合。
- 保留原有单目标导入语义，仍只替换同一目标并保留其他进行中目标。
- 完整校验：23 个测试文件、195 项测试全部通过，生产构建成功。

### 归档目标直接导出

- 已完成目标列表在仍有进行中目标时保持可见，可直接下载目标命名的版本化 JSON 备份和目标证据 Markdown，无需先恢复目标。
- 两种导出都保持归档内容与当前目标选择不变，并增加文件名与非变异交互测试。
- 完整校验：23 个测试文件、191 项测试全部通过，生产构建成功。

### 目标证据审计明细

- 目标证据报告现在按阶段列出每份成果正文、任务与学习日、逐维评估证据和反馈、误解及最小下一步。
- 统一阶段证据归属：补强成果只计入显式来源阶段，避免同阶段重复计数和跨阶段分数污染。
- 增加跨阶段补强归属与报告可追溯性测试；不新增持久化字段或同步状态。
- 完整校验：23 个测试文件、190 项测试全部通过，生产构建成功。

### 目标证据报告

- 计划完成后可导出独立 Markdown 证据报告，集中呈现目标结论、阶段掌握度、四维平均与证据份数。
- 报告纳入补强前后变化和阶段回顾，复用现有确定性派生逻辑，不保存第二份分析状态。
- 增加未完成计划门禁、报告内容和界面下载交互测试。
- 完整校验：23 个测试文件、189 项测试全部通过，生产构建成功。

### 目标优先补强入口

- 目标掌握度汇总现在可直接把自动选出的优先阶段加入当前学习日，或在计划完成后开启对应补强日。
- 目标入口复用阶段补强的来源追踪、每日预算、Evaluator 流程和任务去重，不新增持久化状态。
- 增加跨阶段优先选择、计划外补强日和重复操作保护的界面测试。
- 完整校验：23 个测试文件、186 项测试全部通过，生产构建成功。

## 2026-08-02

### 目标级掌握度汇总

- 汇总所有已完成阶段的掌握度，明确区分计划日程完成与全部阶段证据达标。
- 优先选择缺少评估证据或平均成果最低的待加强阶段，并在界面和学习进展 Markdown 中复用其最小下一步。
- 保持掌握度检查为非阻断建议，学习者仍可继续补强或归档目标。

### 最终阶段补强学习日

- 最终阶段在计划最后一天完成后若仍缺少掌握证据，可直接开启第一个计划外补强学习日，不再被“没有下一天”阻断。
- 补强日复用完整每日任务和时间预算，把阶段最小下一步加入可提交、可评估的实践；完成前暂停归档，完成后将补强证据随目标一起归档。
- 版本 3 校验只接受含有效阶段补强来源的计划外学习日，导出与跨设备同步继续使用同一完整快照。

### 阶段补强效果对比

- 补强任务保存来源阶段、学习日、原任务和原始最小行动，刷新、导出与跨设备同步后仍可追溯。
- 补强评估会回流阶段掌握度检查，展示平均总分和四维均分的前后变化，并更新是否可进入下一阶段。
- 新增补强任务时从当天原子任务重新分配分钟，保持每日总时间预算和版本 3 快照校验成立。
- 聚焦验证：2 个测试文件、66 项测试全部通过，生产构建成功。

### 阶段掌握度检查

- 阶段完成后，从已保存 Evaluator 结果实时汇总理解、应用、证据和反思四维证据，不新增持久化或同步字段。
- 仅在四维平均均至少 3 分且成果平均至少 12/16 时提示可以进入下一阶段；其余情况区分证据不足与需要加强。
- 在阶段回顾和学习进展 Markdown 中展示同一结论，并复用最低分成果的最小下一步。
- 未达标时可把唯一最小下一步加入当前学习日，生成去重的实践任务并复用现有成果提交与四维评估流程。
- 聚焦验证：2 个测试文件、64 项测试全部通过，生产构建成功。
- 完整校验：23 个测试文件、179 项测试全部通过，生产构建成功。

### 跨目标每日执行清单

- 将跨目标总时间预算转化为按恢复、复习风险优先的原子任务清单，并在同级风险下轮转目标。
- 预算只决定今日建议清单，不拆分任务、不改写计划或增加同步状态；可从清单直接打开对应目标与任务。
- 增加预算边界、风险排序、界面跳转与可访问区域测试。
- 完整校验：23 个测试文件、178 项测试全部通过，生产构建成功。

### 跨目标周回顾导出

- 支持把当前跨目标周回顾导出为 Markdown，包含周期、组合投入、逐目标成果与风险变化，以及唯一的本周优先项。
- 导出与界面共用同一确定性派生结果，不增加持久化分析副本。
- 增加 Markdown 内容、文件名和界面下载交互测试。
- 完整校验：23 个测试文件、176 项测试全部通过，生产构建成功。

### 跨目标每日时间预算

- 允许学习者设置或清除当前浏览器的跨目标每日总时间预算，并对全部进行中目标的完整计划分钟数给出预算内或超载差额。
- 超载提示优先建议保留需关注目标和最小学习闭环，不自动修改任务；无效或损坏的本地预算会安全忽略。
- 增加预算派生逻辑、持久化边界和界面交互测试。

### 跨目标学习首页

- 汇总全部进行中目标的今日完成项、剩余时间和需关注数量，并逐个展示恢复或复习风险与最近完成成果。
- 将跨目标摘要收敛为可独立测试的领域派生逻辑，并增加界面交互覆盖。

### 并行目标跨设备同步

- 将浏览器同步 revision 与内容指纹从单计划记录升级为按计划 ID 分区的版本 2 集合，并兼容读取旧版本 1 元数据。
- 自动同步现在协调全部本地活跃目标，并下载其他设备新增的未归档目标；云端存在多个活跃计划不再被误判为目标冲突。
- 批量写回同步结果时保留当前选择，后台更新其他目标不会切换学习界面；归档下载也会排除全部活跃计划 ID。

### 验证

- 完整校验：23 个测试文件、168 项测试全部通过，生产构建成功。

### 本地并行学习目标

- 把单一当前状态扩展为带选择指针的本地活跃目标集合；旧版本 3 单目标数据首次读取时自动迁移并继续镜像兼容键。
- 支持从当前看板新建并行目标、在目标间切换；保存、导入和恢复只更新同一计划，不覆盖其他活跃目标。
- 归档当前目标后自动切换到剩余目标，并明确保留“删除本地数据”清除全部进行中与归档记录的边界。

### 验证

- 完整校验：23 个测试文件、164 项测试全部通过，生产构建成功。

### 已完成目标跨设备归档

- 登录用户完成目标后也可归档；自动同步上传带 `archivedAt` 标记的完整计划和每日证据。
- 同步会下载并校验其他设备上的缺失归档，按计划 ID 合并到本地集合，保留已有本地快照且不把云端归档标记带入可恢复状态。
- 恢复云端归档时持久记录用户的显式恢复意图，以条件写入移除归档标记，避免误判为普通双端编辑冲突。

### 验证

- 完整校验：23 个测试文件、159 项测试全部通过，生产构建成功。

## 2026-08-01

### 跨阶段重复误解

- 从版本 3 Evaluator 历史中发现至少跨两个阶段重复出现的误解，保留来源阶段、学习日和对应最小下一步。
- 只归一大小写、空白和标点，避免把语义相近但并不相同的反馈误合并。
- 在学习界面展示对比式主动回忆提示，并把关联证据与提示加入进展 Markdown 导出；无需数据迁移或新增同步字段。

### 阶段结束回顾

- 阶段最终学习日完成后，可从该阶段成果评估与复盘生成可编辑的目标回顾、代表成果、可迁移能力和下一阶段应用。
- 回顾采用可选计划字段，兼容现有版本 3 学习记录，并随本地备份、进展 Markdown 和跨设备计划同步流转。
- 增加未完成阶段门禁、代表证据选择、编辑持久化和界面交互测试。

### 纵向学习趋势

- 在学习周回顾中增加等长窗口比较，随证据量使用 2–7 个完成日窗口，对比成果评分、偏难日和轻松回忆变化。
- 至少积累 4 个完成日后才归纳“改善、稳定、需要关注”，成果评估缺失时明确显示证据不足。
- 趋势继续从版本 3 学习记录实时派生，并加入学习进展 Markdown 导出，不增加持久化或同步字段。

### 主动回忆自动判分

- 增加独立 Review Agent 契约和 `POST /api/review-assessments`，按回答中的可见证据输出 0–4 分、回忆等级、证据摘要与最小改进动作。
- 复习任务改为提交闭卷答案后自动判分；完整答案和判分证据保存在兼容现有版本 3 数据的复习记录中。
- 自动判分结果继续驱动既有 1、3、7、14 天间隔算法，并在界面即时展示结果和重排后的复习日期。

### 学习进展导出

- 增加单击导出 Markdown，把最近 7 个完成日的周回顾与所有阶段的完成日、评估均分和笔记状态汇总为可携带摘要。

### 完整学习日历

- 增加按自然月导航的学习日历，并用明确状态区分进行中、已完成和无记录日期。
- 日期详情汇总投入时间、平均成果评分、难度反馈和反思；同一自然日的多个学习日保持逐条可见。
- 日历完全从现有版本 3 学习记录派生，不增加持久化字段或同步副本。

### 学习周回顾

- 增加最近 7 个完成日的确定性周回顾，汇总完成日、投入分钟、平均成果评分、偏难日和轻松回忆次数。
- 从最低分成果评估中选择唯一的最小下一步；缺少评估证据或连续偏难时提供相应的行动提示。
- 周回顾完全从版本 3 学习记录派生，不增加新的持久化字段、迁移或同步数据副本。

### 阶段学习笔记

- 增加阶段笔记领域模型和完整校验，从教学讲解、理解回答、实践证据、评估与复盘确定性生成笔记。
- 增加笔记全文检索、标题与正文编辑界面；笔记随学习计划参与本地导出和跨设备同步。
- 生成操作不会覆盖已经人工编辑的阶段笔记，旧版计划缺少笔记字段时继续按空集合兼容读取。
- 增加手动新建阶段笔记，以及按来源学习日去重追加新证据；追加只扩展正文，不覆盖人工标题或已有内容。
- 增加单份阶段笔记 Markdown 导出和二次确认删除；导出包含阶段与来源元数据，删除不影响原始学习记录。

### 复习计划可见性

- 增加未来 14 天薄弱点复习预览，展示来源学习日、预计复习日、误解和最小下一步。
- 预览与每日任务共用同一确定性间隔算法；记录“忘记、费力、轻松”后即时重排，不保存第二份队列。
- 增加领域边界、间隔重排和界面交互测试。

### 模型与 Agent

- 增加 `OPENAI_COMPATIBLE_API_KEY`、`OPENAI_COMPATIBLE_BASE_URL` 和兼容模型配置支持。
- 兼容服务支持服务根地址或 `/v1` 地址，统一调用 Chat Completions + JSON Schema。
- 将测试模型切换为 `gpt-5.6-luna`。
- 在真实 API 环境完成 Planner Agent 烟雾测试：返回有效的 2 阶段、4 任务、30 分钟计划。
- 实现 Coach Agent 学习中断恢复闭环：识别至少两个空档日或连续两天偏难，并按用户请求生成 10–20 分钟低压力重启计划。
- 增加 provider-neutral 恢复计划 Schema、领域校验、确定性开发实现和 `POST /api/recovery-plans`。

### OIDC、同步与测试基础设施

- 在 `dev` 部署 Dex OIDC、本地 PostgreSQL 数据库和每日 7 天滚动备份。
- 验证 OIDC 授权码 + PKCE、登录、自动同步、手动同步和退出流程。
- 增加本地开发用 HTTP issuer 支持；生产环境仍要求 HTTPS。
- 增加测试环境账号、设备会话和本机 SSH 隧道访问方式。

### Web/API 部署

- 增加 Web/API 容器配置、Nginx 代理配置和用户级 systemd 服务配置。
- 因 `dev` 到 Docker Hub 的 IPv6 拉取超时，改用服务器 NVM 的 Node 22.21.1 运行 Web/API 服务；Dex/PostgreSQL 继续使用 Docker。
- 远端服务只监听回环地址，本机通过 `127.0.0.1:8088` SSH 隧道访问。
- 远端健康检查通过，实时模型、OIDC 和同步均已启用。

### 验证

- 完整测试：21 个测试文件、111 项测试全部通过。
- 生产构建成功。
- 测试环境健康端点返回 `status: ok`、`aiEnabled: true`、`syncEnabled: true`。
- Coach Agent 聚焦测试 4 个文件、43 项全部通过，生产构建成功；完整项目校验结果见本次提交记录。
- 本次完整校验：22 个测试文件、124 项测试全部通过，生产构建成功。
- 阶段笔记更新后完整校验：22 个测试文件、126 项测试全部通过，生产构建成功。
- 手动笔记与证据追加后完整校验：22 个测试文件、127 项测试全部通过，生产构建成功。
- 笔记 Markdown 导出与确认删除后完整校验：22 个测试文件、129 项测试全部通过，生产构建成功。
- 复习计划预览后完整校验：22 个测试文件、130 项测试全部通过，生产构建成功。
- 学习周回顾聚焦验证：2 个测试文件、39 项测试全部通过；完整校验结果见本次提交。
- 学习周回顾完成后完整校验：22 个测试文件、133 项测试全部通过，生产构建成功。
- 完整学习日历完成后完整校验：22 个测试文件、135 项测试全部通过，生产构建成功。
- 主动回忆自动判分与学习进展导出完成后完整校验：23 个测试文件、143 项测试全部通过，生产构建成功。
- 纵向学习趋势完成后完整校验：23 个测试文件、146 项测试全部通过，生产构建成功。
- 阶段结束回顾完成后完整校验：23 个测试文件、148 项测试全部通过，生产构建成功。
- 跨阶段重复误解聚焦验证：2 个测试文件、51 项测试全部通过，生产构建成功；完整校验结果见本次提交。

### 内部流程

- 建立内部更新日志，后续每次更新记录功能、部署、验证和风险。
- 发现一次远端依赖目录被清空导致服务重启失败，使用服务器 NVM 重新执行 `npm ci` 后恢复；部署流程增加依赖存在性检查。

## 后续记录格式

每次更新追加一个日期小节，至少包含：

- 更新内容：新增功能、修复或配置变化
- 部署变化：是否同步到 `dev`，运行方式是否变化
- 验证结果：测试、构建、健康检查和手工验证
- 待办或风险：仍需用户处理的凭据、外部服务或上线事项
